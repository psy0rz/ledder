//dummy for testing
import {Matrix} from "../../ledder/Matrix.js";

export class MatrixDummy extends Matrix {
    constructor(width, height) {
        super(width, height);
    }

    setPixel(x, y, color) {
    }

    frame() {
    }
}
